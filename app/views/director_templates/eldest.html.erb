<html>
<head>
  <title>Target: MSM Queries</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
</head>
<body>
  <nav>
    <a href="/">Home</a>
  </nav>
  <h1>Eldest director</h1>
  <p>The eldest director in our dataset right now is <a href="/directors/<%= @eldest_director.id %>"><%= @eldest_director.name %></a>, born on <%= @eldest_director.dob.strftime("%B %d, %Y") %>.</p>
  <p>(<a href="http://strftime.net/" target="_blank">http://strftime.net/</a> is handy for putting together the formatting strings for the <code>.strftime</code> method.)</p>
</body>
</html>
